# 📋 Команды для публикации на GitHub

## 🚀 Быстрый старт (скопируйте и выполните)

```powershell
# Перейдите в папку проекта
cd "c:\Users\rikty\OneDrive\Документы\Python\Screenshot"

# 1. Инициализация Git
git init

# 2. Добавление всех файлов
git add .

# 3. Создание первого коммита
git commit -m "Initial commit: Office2Img v1.0.0"

# 4. Переименование ветки в main
git branch -M main

# 5. Добавление удаленного репозитория
git remote add origin https://github.com/kapibas/office2img.git

# 6. Отправка на GitHub
git push -u origin main
```

## ⚠️ Важно: Сначала создайте репозиторий на GitHub!

1. Откройте: **https://github.com/new**
2. Заполните:
   - **Repository name**: `office2img`
   - **Description**: `Конвертер документов Office в изображения (PDF, DOCX, PPTX → PNG/JPG)`
   - Выберите **Public**
   - **НЕ** ставьте галочки на "Add a README file", ".gitignore" или "license"
3. Нажмите **"Create repository"**

## 🔐 Авторизация

При выполнении `git push` GitHub попросит авторизоваться:

### Способ 1: Personal Access Token (рекомендуется)

1. Создайте токен: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Выберите срок действия и права (нужен `repo`)
4. Скопируйте токен
5. При запросе пароля вставьте токен вместо пароля

### Способ 2: GitHub CLI

```powershell
# Установите GitHub CLI
winget install --id GitHub.cli

# Авторизуйтесь
gh auth login

# Затем выполните git push
git push -u origin main
```

## ✅ Проверка

После успешной публикации откройте:
**https://github.com/kapibas/office2img**

## 🏷️ Создание первого релиза (опционально)

```powershell
# Создать тег
git tag -a v1.0.0 -m "Первая версия Office2Img"

# Отправить тег
git push origin v1.0.0
```

Затем на GitHub создайте Release через веб-интерфейс.

## 📝 Если что-то пошло не так

### Ошибка "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/kapibas/office2img.git
```

### Ошибка "nothing to commit"
```powershell
# Проверьте статус
git status

# Если файлы не добавлены
git add .
git commit -m "Initial commit: Office2Img v1.0.0"
```

### Ошибка авторизации
Используйте Personal Access Token (см. выше) вместо пароля.

---

**Готово!** 🎉 Ваш проект теперь на GitHub.
